#pragma once

#include <SFML/Graphics.hpp>

#include <vector>

#include "GameObj.h"


class MoveAble : public GameObj
{
public:
	using GameObj::GameObj;
	//MoveAble(const sf::Texture& texture, const sf::Vector2f& position);
	//virtual void move();
	//virtual void handleCollis(const GameObj* object);
};